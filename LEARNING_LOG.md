# Журнал обучения

После занятия копируй шаблон вниз файла и заполняй 3–5 минут.

## Шаблон

### ГГГГ-ММ-ДД — тема

- Время:
- Что сделал:
- Что понял своими словами:
- Где застрял:
- Следующий маленький шаг:

---

## 2026-07-13 — старт системы

- Время: подготовка рабочего пространства.
- Что сделал: сформулировал цель — первый оффер в аналитике данных / Data Science.
- Что понял своими словами: предстоит заполнить после знакомства с Codex.
- Где застрял: локально установлен устаревший 32-битный Python 3.8; Git пока не найден.
- Следующий маленький шаг: настроить инструменты и решить первую функцию в проекте.

## 2026-07-13 — настройка среды

- Время: установка и автоматическая проверка инструментов.
- Что сделал: установлен Python 3.12 x64, Git, JupyterLab и основные Data Science-библиотеки; создано окружение `.venv` и Git-репозиторий.
- Что понял своими словами: `.venv` изолирует библиотеки проекта, а тесты показывают конкретные требования к коду.
- Где застрял: функции первого задания пока содержат `NotImplementedError` — это намеренная стартовая точка.
- Следующий маленький шаг: реализовать только `weight_gain` и добиться прохождения двух связанных проверок.

## 2026-07-14 — Python, Git и виртуальное окружение

- Время: [14:00-16:00]
- Что сделал:

  - добавил ввод начального и текущего веса;
  - преобразовал текст из `input()` в числа через `float`;
  - обработал неправильный ввод через `try/except`;
  - активировал и деактивировал виртуальное окружение `.venv`;
  - самостоятельно выполнил `status`, `diff`, `add`, `commit` и `push`;
  - отправил изменения на GitHub.

- Что понял своими словами:

  - `input()` всегда возвращает текст, поэтому перед вычислениями его нужно преобразовать в число;
  - `try` выполняет потенциально опасный код, а `except ValueError` обрабатывает ожидаемую ошибку преобразования;
  - `.venv` хранит отдельные библиотеки проекта и помогает не смешивать зависимости;
  - `git add` помещает выбранную версию файла в staging area;
  - `git commit` сохраняет содержимое staging area в локальной истории;
  - `git push` отправляет локальные коммиты на GitHub.

- Где застрял:

  - сначала не понимал принципиальную разницу между `git add` и `git commit`;
  - команда `git` не находилась через `PATH`, поэтому использовал полный путь;
  - PowerShell запрещал запуск `Activate.ps1`;
  - не сразу понял, что `(END)` означает режим просмотра длинного вывода Git.

- Что помогло:

  - аналогия staging area с подготовкой кадра перед фотографией;
  - чтение `git status` и `git diff`;
  - временная политика `RemoteSigned` только для текущего терминала;
  - проверка пути через `sys.executable`.

- Следующий маленький шаг:
  - создать первый CSV-файл с датой и весом;
  - прочитать его обычным Python;
  - понять строки, столбцы и табличные данные.

## 2026-07-14 — списки, индексы и тестирование

- Время: [19:30-20:00]

- Что сделал:

  - изучил положительные и отрицательные индексы списка;
  - создал функцию `weight_gain_from_history`;
  - переиспользовал существующую функцию `weight_gain`;
  - добавил проверку истории короче двух измерений;
  - добавил автоматические тесты для обычного и ошибочных случаев;
  - использовал Ruff для автоматического форматирования кода.

- Что понял своими словами:

  - `weights[0]` означает первый элемент списка;
  - `weights[-1]` означает последний элемент списка;
  - переиспользование функции нужно, потому что одна и та же логика не дублируется в разных местах и её проще исправлять;
  - проверка `len(weights) < 2` нужна, потому что при выполнении функции необходимо минимум 2 элемента в списке;
  - автоматический тест отличается от ручной проверки тем, что сам запускает заранее заданный сценарий и сравнивает полученный результат с ожидаемым.

- Где застрял:

  - путался в положительных и отрицательных индексах;
  - не сразу выставил правильные отступы внутри теста;
  - сначала попытался импортировать функцию как отдельный модуль;
  - встроенный просмотрщик Codex не смог обновить изменившийся файл.

- Что помогло:

  - схема индексов списка;
  - чтение текста `IndentationError` и `ModuleNotFoundError`;
  - разделение рабочего кода и тестов;
  - автоматическое форматирование через Ruff.

- Следующий маленький шаг:

  - создать CSV-файл с датой и весом;
  - прочитать его обычным Python;
  - познакомиться со строками и столбцами табличных данных.
